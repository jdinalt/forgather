# Dynamic Loader Whitelist
# Only constructors listed here are allowed in the configuration file.
# This is intended to prevent the arbitrary execution of any Python code by
# a configuration script. Be very careful what you add when running
# a confiuration from an untrusted source!

# Transformers
- transformers:AutoTokenizer.from_pretrained
# TODO: AutoModel.from_pretrained has a 'trust_remote_code' flag; deal with this.
- transformers:AutoModel.from_pretrained
- transformers:DataCollatorForLanguageModeling
- transformers:Trainer
- transformers:TrainingArguments

# Accelerate
- accelerate:DataLoaderConfiguration

# Datasets
- datasets:load_from_disk
# TODO: load_dataset has a 'trust_remote_code' flag; deal with this.
- datasets:load_dataset

# Torch
- torch.utils.tensorboard:SummaryWriter

# AIWS
- aiws.trainer:Trainer
- aiws.trainer_types:TrainingArguments
- aiws.accel_trainer:AccelTrainer
- aiws.accel_trainer:AccelTrainingArguments
- aiws.default_callbacks:InfoCallback
- aiws.default_callbacks:ProgressCallback
- aiws.default_callbacks:JsonLogger
- aiws.tb_logger:TBLogger
- aiws.construct:register_for_auto_class

# forgather
- forgather.construct:get_item
- forgather.construct:flatten
- forgather.construct:get_attr