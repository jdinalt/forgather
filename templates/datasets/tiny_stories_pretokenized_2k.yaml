-- extends 'datasets/pretokenized_dataset.yaml'

-- block datasets_meta_config
    == super()
    -- set datasets_def.name = "Tiny Stories 2K"
    -- set datasets_def.description = "Tiny Stories dataset, pre-tokenized with the tiny_2k tokenizer."
    -- set datasets_def.dataset_name_or_path = path_join(ns.DATASETS_DIR, 'tiny_stories_tokenized')
-- endblock datasets_meta_config