-- extends 'project.yaml'

-- block experiment_metadata
    == super()
    -- set ns.EXPERIMENT_NAME = "Default Trainer"
    -- set ns.EXPERIMENT_DESCRIPTION = "Train with aiws.trainer.Trainer implementation"
    -- set ns.LOG_NAME = "default_trainer"

    ## Set trainer; this is a variable defined in 'projects/tiny.yaml'
    -- set ns.trainer_class = 'trainers/trainer.yaml'
-- endblock experiment_metadata