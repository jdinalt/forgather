-- set experiment.ASSETS_DIR = path_join('..')
-- set experiment.TEMPLATES_DIR = path_join(experiment.ASSETS_DIR, 'templates')
-- set experiment.PROJ_TEMPLATES_DIR = 'forgather_demo'
-- set experiment.TOKENIZERS_DIR = path_join(experiment.ASSETS_DIR, 'tokenizers')
-- set experiment.DATASETS_DIR = path_join(experiment.ASSETS_DIR, 'datasets')
-- set experiment.SCRIPTS_DIR = path_join(experiment.ASSETS_DIR, 'scripts')
-- set experiment.MODEL_ZOO_DIR = path_join(experiment.ASSETS_DIR, 'model_zoo')
-- set experiment.MODEL_SRC_DIR = experiment.MODEL_ZOO_DIR
-- set experiment.MODELS_DIR = path_join("forgather_demo", "output_models")