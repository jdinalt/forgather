-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "My Finetune Cofiguration"
    -- set ns.config_description = "Finetune a model on stuff!"
    -- set ns.log_name = "default"
-- endblock config_metadata


-- block trainer_definition
    -- include 'default.trainer_config'
-- endblock trainer_definition


-- block construct_new_model
    -- include 'default.model_config'
-- endblock construct_new_model

#-------------------- default.trainer_config --------------------
-- extends 'project.trainer_config'

-- block trainer_args
    == super()
    # Experiment overrides
-- endblock trainer_args
