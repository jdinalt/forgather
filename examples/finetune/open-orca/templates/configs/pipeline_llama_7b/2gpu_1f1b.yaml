-- extends 'project.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Open Orca Llama 7B 1F1B Pipe x2"
    -- set ns.config_description = "1F1B 2 GPU Pipeline"
    -- set ns.log_name = "1f1b_2gpu"
    -- set ns.trainer_class = 'trainers/pipeline/1f1b_2gpu.yaml'

[trainer_definition]
    -- include 'config.trainer_config'

[datacollator]
    == super()
    padding: "max_length" # Required for pipeline
    max_length: 1300

#-------------------- config.trainer_config --------------------
-- extends 'project.trainer'

[trainer_args]
    == super()
    # **config**