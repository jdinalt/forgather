-- extends 'samantha.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Samantha Llama3 1B"
    -- set ns.config_description = "Conservative setting to train a 1B Llama model on 1 GPU"
    -- set ns.log_name = "default"

    ## Batching
    -- set ns.seq_len = seq_len | default(4096)
    -- set ns.per_device_train_batch_size = batch_size | default(2)

    ## Compile settings
    -- set ns.torch_compile = compile | default(False)