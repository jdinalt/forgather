-- extends 'samantha.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Samantha Llama7B Medium"
    -- set ns.config_description = "Train with 1300 token context on single GPU, 24 GBs"
    -- set ns.log_name = "1gpu_1300"

[trainer_definition]
    -- include 'config.trainer_config'

[datacollator]
    == super()
    # This is about as long as is needed for Samantha...
    # This should work on a 24 GB GPU
    max_length: 1300
    ## Set to always use max_length; useful for profiling, to avoid surprises.
    ## adding: "max_length"

#-------------------- config.trainer_config --------------------
-- extends 'project.trainer'

[trainer_args]
    == super()
   
    # **config**
    gradient_checkpointing: True
    fuse_optim_with_backward: True

