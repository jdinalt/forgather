-- extends 'samantha.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Samantha Llama 7B"
    -- set ns.config_description = "Conservative setting to train a 7B Llaama model on 1 GPU"
    -- set ns.log_name = "default"
-- endblock config_metadata


-- block trainer_definition
    -- include 'default.trainer_config'
-- endblock trainer_definition


-- block datacollator
    == super()
    max_length: 512
-- endblock datacollator


#-------------------- default.trainer_config --------------------
-- extends 'trainers/base_finetune_trainer.yaml'


-- block trainer_args
    == super()
    # Config overrides
    # Setting for llama 7B on single GPU (16 GB)
    max_grad_norm: null
    gradient_checkpointing: {{ gradient_checkpointing | default(True) }}
    fuse_optim_with_backward: True
-- endblock trainer_args

