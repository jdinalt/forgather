## Note: This project assumes that the model has manual activation checkpointing
## enabled!
## TODO: Get this this to work!
-- extends 'samantha.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Samantha Llama 7B DDP"
    -- set ns.config_description = "Train a 7B Llaama model on N GPUs via DDP"
    -- set ns.log_name = "ddp"
    -- set ns.trainer_class = 'trainers/accel_trainer.yaml'
-- endblock config_metadata

-- block datacollator
    == super()
    # Limit sequence length to avoid OOM
    max_length: 512
-- endblock datacollator

-- block trainer_definition
    -- include 'default.trainer_config'
-- endblock trainer_definition

#-------------------- default.trainer_config --------------------
-- extends 'samantha.trainer_config'

-- block trainer_meta_config
    == super()
    ## Enable DDP training, when supported
    -- set ns.nproc_per_node = "gpu"
<< endblock trainer_meta_config


-- block trainer_args
    == super()
    # Experiment overrides
    max_grad_norm: null
    fuse_optim_with_backward: True
-- endblock trainer_args
