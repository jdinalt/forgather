-- extends 'samantha.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Samantha Llama 7B 1F1B Pipe x2"
    -- set ns.config_description = "Train Samantha on 1F1B 2 GPU Pipeline"
    -- set ns.log_name = "1f1b_2gpu"
    -- set ns.trainer_class = 'trainers/pipeline/1f1b_2gpu.yaml'

[trainer_definition]
    -- include 'config.trainer_config'

[datacollator]
    == super()
    padding: "max_length" # Required for pipeline
    max_length: 1024

#-------------------- config.trainer_config --------------------
-- extends 'project.trainer'

[trainer_args]
    == super()

    # **config**
