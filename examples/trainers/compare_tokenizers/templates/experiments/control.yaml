## The project defines the defaults
-- extends 'project.yaml'

-- block config_metadata
    == super()
    ## Experiment Metadata
    -- set ns.config_name = "Control"
    -- set ns.config_description = "This regenerates the exmaple tiny-stories 2k tokenizer from scratch."
    -- set ns.log_name = "2k"
-- endblock config_metadata
