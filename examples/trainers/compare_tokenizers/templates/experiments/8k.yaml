-- extends 'project.yaml'

-- block config_metadata
    == super()
    ## Experiment Metadata
    -- set ns.config_name = "8k tokenizer"
    -- set ns.config_description = "tiny_2k_bpe tokenizer definition with 8k tokens; this results in a much larger model!"
    -- set ns.log_name = "8k"
    -- set ns.tokenizer_project_config = "8k.yaml"
    -- set ns.max_new_tokens = 20
-- endblock config_metadata