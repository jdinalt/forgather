-- extends 'project.yaml'

-- block config_metadata
    == super()
    ## Experiment Metadata
    -- set ns.config_name = "Byte Tokenizer"
    -- set ns.config_description = "tiny_2k_bpe, reduced to the base 256 byte tokens + special tokens."
    -- set ns.log_name = "bytes"
    -- set ns.tokenizer_project_config = "256.yaml"
    -- set ns.max_new_tokens = 240
-- endblock config_metadata