## The project defines the defaults
-- extends 'project.yaml'

-- block experiment_metadata
    == super()
    ## Experiment Metadata
    -- set ns.EXPERIMENT_NAME = "Control"
    -- set ns.EXPERIMENT_DESCRIPTION = "This regenerates the exmaple tiny-stories 2k tokenizer from scratch."
    -- set ns.log_name = "2k"
-- endblock experiment_metadata
