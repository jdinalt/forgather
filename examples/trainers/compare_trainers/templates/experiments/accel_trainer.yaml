-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Accelerate Trainer"
    -- set ns.config_description = "Train with forgather.ml.accel_trainer.AccelTrainer implementation"
    -- set ns.log_name = "accel_trainer"

    ## Set trainer
    -- set ns.trainer_class = 'trainers/accel_trainer.yaml'
-- endblock config_metadata