-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Torch Flash Adafactor Stochastic Rounding"
    -- set ns.config_description = ""
    -- set ns.model_name = "flash"
    -- set ns.log_name = "torch_flash_adafactor_sr"
-- endblock config_metadata


-- block construct_new_model
    -- include 'experiment.model'
-- endblock construct_new_model

-- block optimizer
.define: &optimizer !lambda:forgather.ml.optim.adafactor:Adafactor
    lr: 1.0e-3
    weight_decay: 0.01
    bf16_stochastic_round: True
<< endblock optimizer

#-------------------- experiment.model --------------------
-- extends 'project.model_flash'

-- block model_config
    == super()
    attention_type: "torch_flash"
<< endblock model_config