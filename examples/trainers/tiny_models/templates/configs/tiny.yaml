-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Tiny"
    -- set ns.config_description = "A tiny vanilla transformer."
    -- set ns.model_name = "tiny"
    -- set ns.log_name = "vanilla"
-- endblock config_metadata


-- block construct_new_model
    -- include 'experiment.model_config'
-- endblock construct_new_model

#-------------------- experiment.model_config --------------------
-- extends 'models/tiny/tiny.yaml'