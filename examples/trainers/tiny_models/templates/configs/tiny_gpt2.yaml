-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Tiny GPT2"
    -- set ns.config_description = "A tiny GPT2 model."
    -- set ns.model_name = "tiny_gpt2"
    -- set ns.log_name = "gpt2"
-- endblock config_metadata


-- block construct_new_model
    -- include 'experiment.model_config'
-- endblock construct_new_model

#-------------------- experiment.model_config --------------------
-- extends 'models/tiny/tiny_gpt2.yaml'