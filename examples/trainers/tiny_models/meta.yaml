-- set templates_dir = joinpath('..', '..', '..', 'templates')

searchdir:
    - "templates"
    - "{{ joinpath(templates_dir, 'tiny_experiments') }}"
    - "{{ joinpath(templates_dir, 'modellib') }}"
    - "{{ joinpath(templates_dir, 'base') }}"

config_prefix: "configs"
default_config: "tiny_causal.yaml"