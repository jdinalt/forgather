-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Swish"
    -- set ns.config_description = "Replace the activation function with 'Swish' function"
    -- set ns.model_name = "swish"
    -- set ns.log_name = "swish"
-- endblock config_metadata


-- block construct_new_model
    -- include 'experiment.model_config'
-- endblock construct_new_model

#-------------------- experiment.model_config --------------------
-- extends 'project.model_config'

-- block feedforward_factory
    == super()
    # Replace the default activation ReLU with SiLU (Swish)
    # https://pytorch.org/docs/stable/generated/torch.nn.SiLU.html#torch.nn.SiLU
    activation: !factory:torch.nn:SiLU []
-- endblock feedforward_factory