-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Control"
    -- set ns.config_description = "Tiny Causal; the baseline control"
    -- set ns.model_name = "tiny_causal"
    -- set ns.log_name = "control"
-- endblock config_metadata


-- block construct_new_model
    -- include 'experiment.model_config'
-- endblock construct_new_model

#-------------------- experiment.model_config --------------------
-- extends 'project.model_config'