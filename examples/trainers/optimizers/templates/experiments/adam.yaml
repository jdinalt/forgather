-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Torch Adam"
    -- set ns.config_description = "Torch Adam; the baseline control"
    -- set ns.log_name = "adam"
-- endblock config_metadata

-- block optimizer
.define: &optimizer !lambda:torch:optim.AdamW
    lr: {{ns.lr}}

<< endblock optimizer