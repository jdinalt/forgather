-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "SGD"
    -- set ns.config_description = "Torch SGD"
    -- set ns.log_name = "my_sgd"
-- endblock config_metadata

-- block optimizer
.define: &optimizer !lambda:./src/sgd.py:SGD
    lr: 1.0e-1

<< endblock optimizer