-- extends 'configs/lovecraft.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Lovecraft Long Context Packed"
    -- set ns.config_description = "Densely packed blocks for honest long-context memory probes (no padding)"

[map_function]
.define: &map_function !partial:forgather.ml.datasets:block_tokenize_fn
    max_length: {{ window_size | default(4096) }}
    stride: 0
    packed: True
    packing_strategy: "greedy"
