-- extends "project.yaml"

[config_metadata]
    == super()
    -- set ns.config_name = "DDP"
    -- set ns.config_description = "Test 2 GPU with 10 accumulation steps using DDP"
    -- set ns.log_name = "fsdp"
    -- set ns.nproc_per_node = 2

[parallelism]
    == super()
    data_parallel_replicate_degree: 1
    data_parallel_shard_degree: 2