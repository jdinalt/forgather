-- extends "project.yaml"

[config_metadata]
    == super()
    -- set ns.config_name = "TP"
    -- set ns.config_description = "Test 2 GPU with 10 accumulation steps using TP"
    -- set ns.log_name = "tp"
    -- set ns.nproc_per_node = 2

[parallelism]
    == super()
    data_parallel_shard_degree: 1
    tensor_parallel_degree: 2

[data_collator]
    == super()
    # Tensor Parallel requires constant input tensor shapes
    padding: "max_length"
    truncation: True
