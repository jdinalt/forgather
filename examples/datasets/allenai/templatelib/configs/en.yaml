-- extends "datasets/load_packed_dataset.yaml"

[config_metadata]
    == super()
    -- set ns.config_name = "C4"
    -- set ns.config_description = "A colossal, cleaned version of Common Crawl's web crawl corpus. Based on Common Crawl dataset https://commoncrawl.org"
    -- set ns.source = "https://huggingface.co/datasets/allenai/c4"

[load_dataset_args]
    == super()
    path: "allenai/c4"
    name: "en"

[validation_dataset_split]
    == super()
    split: "validation[:1000]"

[test_dataset_split]
    == super()
    split: "validation[1000:100000]"