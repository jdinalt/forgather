-- extends "configs/openorca.yaml"
-- from "packed.yaml" import map_function, map_kwargs, dynamic_args

[config_metadata]
    == super()
    -- set ns.config_name = "Open Orca Packed"
    -- set ns.config_description = "Open Orca with best-fit sequence packing."

[map_function]
{{ map_function(ns.orca_src, ns.chat_template, max_length | default(512), stride | default(0)) }}

[map_args]
    == super()
{{ map_kwargs() }}

[dynamic_args]
    == super()
{{ dynamic_args() }}
