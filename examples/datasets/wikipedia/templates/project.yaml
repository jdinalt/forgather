tokenizer: &tokenizer !singleton:transformers:AutoTokenizer.from_pretrained@tokenizer
    - "{{ tokenizer_path | default('undefined') }}"

-- include "project.dataset"

meta: !dict
    config_name: "Wikepedia Dataset"
    config_description: "Wikipedia dataset containing cleaned articles from the English Wikipedia."
    config_class: "type.dataset"

#-------------------- project.dataset --------------------
-- extends "datasets/wikipedia.yaml"