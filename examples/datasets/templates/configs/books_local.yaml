-- extends 'types/dataset/dataset_wrapper.yaml'

-- block config_metadata
    == super()
    ## For a local dataset, you will need to set this to point at your datasets directory.
    -- set ns.datasets_dir = joinpath(user_home_dir(), "ai_assets", "datasets")
    ## These should point at the directories with the local datasets
    -- set ns.datasets_train_path = joinpath(ns.datasets_dir, "togethercomputer-RedPajama-Data-1T-book")
    -- set ns.datasets_eval_path = joinpath(ns.datasets_dir, "red_pajamas_books_validation")
-- endblock config_metadata


-- block dataset
    -- include 'datasets/books/books_local.yaml'
-- endblock dataset
