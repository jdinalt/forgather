-- extends "configs/openthoughts.yaml"
-- from "packed.yaml" import map_function, map_kwargs, dynamic_args

[config_metadata]
    == super()
    -- set ns.config_name = "OpenThoughts3 Packed"
    -- set ns.config_description = "OpenThoughts3 with best-fit sequence packing."

[map_function]
{{ map_function(ns.openthoughts_src, ns.chat_template, max_length | default(512), stride | default(0)) }}

[map_kwargs]
    == super()
{{ map_kwargs() }}

[dynamic_args]
    == super()
{{ dynamic_args() }}
