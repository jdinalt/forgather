-- extends "configs/Cpp-Code-Large.yaml"
-- from "packed.yaml" import map_function, map_kwargs, dynamic_args

[config_metadata]
    == super()
    -- set ns.config_name = "C++ Code Large Packed"
    -- set ns.config_description = "C++ Code Large with packed sequences"

[map_function]
{{ map_function(max_length | default(512), stride | default(0)) }}

[map_kwargs]
    == super()
{{ map_kwargs() }}

[dynamic_args]
    == super()
{{ dynamic_args() }}
