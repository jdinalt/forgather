-- extends "configs/tinystories.yaml"

[config_metadata]
    == super()
    -- set ns.config_name = "Tiny Stories Iterable"
    -- set ns.config_description = "The main dataset, but configured as an IterableDataset, for testing with this dataset type."

[train_dataset]
    == super()
    to_iterable: True
