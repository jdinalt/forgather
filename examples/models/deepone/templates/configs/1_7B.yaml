-- extends "models/model_type.yaml"

[config_metadata]
    == super()
    -- set ns.config_name = "Base Deepone"
    -- set ns.config_description = "Default Deepone configuration"
    -- set ns.model_name = "1_7b"

[model_definition]
    -- include "config.model"

#------------- config.model --------------
-- extends "models/transformers/deepone.yaml"
