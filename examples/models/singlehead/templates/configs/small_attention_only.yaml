-- extends "project.yaml"

[config_metadata]
    == super()
    -- set ns.config_name = "Singlehead Small (attention-only)"
    -- set ns.config_description = "Attention-only single-head ALiBi transformer (no MLP), ~8M params"
    -- set ns.model_name = model_name | default("singlehead_small_attn_only")
    ## Drop the MLP -- the paper-faithful, attention-only transformer.
    -- set ns.use_mlp = False

[model_definition]
    -- include "config.small.model"

#------------- config.small.model --------------
-- extends "project.model"

[model_config]
    == super()
    hidden_size: {{ hidden_size | toyaml(512) }}
    num_hidden_layers: {{ num_hidden_layers | toyaml(8) }}
    intermediate_size: {{ intermediate_size | toyaml(1280) }}
