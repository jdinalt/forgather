# Example Models

Self-contained model definitions shared across training projects. Each project here
defines a specific model architecture or variant; they are referenced from training
projects via `ns.model_project_dir`.
