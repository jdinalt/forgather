-- extends "meta_defaults.yaml"

-- block configs
name: "LlamaCanon"
description: "Llama with Canon layers (depthwise causal convolutions for local token mixing)"
config_prefix: "configs"
default_config: "canon.yaml"
<< endblock configs
