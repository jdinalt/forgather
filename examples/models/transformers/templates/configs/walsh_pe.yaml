-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Walsh PE"
    -- set ns.config_description = "Hadamard-Walsh Positional Encoder"
    -- set ns.model_name = "walsh"
-- endblock config_metadata


-- block model_definition
    -- include 'experiment.model_config'
-- endblock model_definition

#-------------------- experiment.model_config --------------------
-- extends 'models/dynamic_lm.yaml'

-- block positional_encoder_factory
# Note: This is the author's own creation; there is no literature to cite.
# See notes in implementation for details.
.define: &positional_encoder_factory !factory:.walsh_pe:WalshPE
    d_model: !var "hidden_size"
    max_sequence_length: !var "max_sequence_length"
-- endblock positional_encoder_factory