-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Default Dynamic Causal Transformer"
    -- set ns.config_description = ""
    -- set ns.model_name = 'dynamic_causal_transformer'
-- endblock config_metadata


-- block model_definition
    -- include 'models/dynamic_causal_transformer.yaml'
-- endblock model_definition