## The project defines the defaults
-- extends 'project.yaml'

[config_metadata]
    == super()
    ## Experiment Metadata
    -- set ns.config_name = 'My ML Science Project'
    -- set ns.config_description = "It's not supid, it's advanced!"
    -- set ns.model_name = 'my_custom_model'
    -- set ns.log_name = "control"


## Make some changes to the default trainer definition
[trainer_definition]
    -- include 'custom_model.trainer_config'

## Override default project definition with custom model
[construct_new_model]
    -- include 'custom_model.model_config'

## The next line defines a new template within the same file.
#-------------------- custom_model.trainer_config --------------------
-- extends 'project.trainer_config'

[trainer_args]
    == super()
    #  **custom_model**

    # Increase effective batch-size by 4x
    gradient_accumulation_steps: 4

#-------------------- custom_model.model_config --------------------
-- extends 'models/example_model_config.yaml'

[model_config]
    == super()
    # ** Custom Model **
    # Make this a little deeper than the defaults
    num_hidden_layers: 6