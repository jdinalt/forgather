-- extends "projects/lm_training_project.yaml"

[config_metadata]
    == super()
    -- set ns.config_name = "PP"
    -- set ns.config_description = "Test PP Trainer"
    -- set ns.trainer_type = trainer_type | default("pipeline")
    -- set ns.nproc_per_node = 2