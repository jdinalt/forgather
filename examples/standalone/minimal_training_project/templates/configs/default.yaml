-- extends "training_script/causal_lm/causal_lm.yaml"

## Set Project level defaults
-- block config_metadata
    == super()
    -- set ns.config_name = "Minimal Training Project"
    -- set ns.config_description = "A bare-bones training project config, using defaults, where available."
    
    ## Import a dataset project -- use iterable version of Tiny Stories
    -- set ns.dataset_proj = dataset_proj | default(joinpath(ns.forgather_dir, 'examples', 'datasets', 'roneneldan'))
    -- set ns.dataset_config = dataset_config | default("tinystories-iter.yaml")

    ## Import a model project -- use a tiny vanilla transformer
    -- set ns.model_project_dir = joinpath(ns.forgather_dir, 'examples', 'models', 'causal_lm')
    -- set ns.model_project_config = "tiny.yaml"
<< endblock config_metadata


-- block construct_new_model
    -- include "models/causal_lm/import_model_project.yaml"
<< endblock construct_new_model


-- block datasets_definition
    -- include 'datasets/llm_dataset_project.yaml'
<< endblock datasets_definition
