-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.tokenizer_name = "tiny_stories_2k"
    -- set ns.CONFIG_NAME = ns.CONFIG_NAME + " 2K"
    -- set ns.CONFIG_DESCRIPTION = ns.CONFIG_DESCRIPTION + " w/ 2K tokens"
    -- set ns.vocab_size = 2000
-- endblock config_metadata