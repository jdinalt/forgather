-- extends 'project.yaml'

[config_metadata]
    == super()
    -- set ns.tokenizer_name = tokenizer_name | default("tiny_stories_8k")
    -- set ns.config_name = ns.config_name + " 8K"
    -- set ns.config_description = ns.config_description + " w/ 8K tokens"
    -- set ns.vocab_size = vocab_size | default(8000)