-- extends 'types/tokenizer/bpe/bpe.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Tiny Stories"
    -- set ns.config_description = "BPE tokenizer trained on Tiny Stories dataset"
    -- set ns.dataset_id = 'roneneldan/TinyStories'
    -- set ns.model_max_length = 2048
-- endblock config_metadata


