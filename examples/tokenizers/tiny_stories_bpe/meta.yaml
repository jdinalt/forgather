-- set templates = joinpath('..', '..', '..', 'templates')

searchdir:
    - "templates"
    - "{{ joinpath(templates, 'tiny_experiments') }}"
    - "{{ joinpath(templates, 'base') }}"

config_prefix: "configs"
default_config: "2k.yaml"