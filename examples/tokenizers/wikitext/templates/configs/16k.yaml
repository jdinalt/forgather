-- extends 'project.yaml'

[config_metadata]
    == super()
    -- set ns.tokenizer_name = tokenizer_name | default("wikitext_16k")
    -- set ns.config_name = "Wikitext 16K"
    -- set ns.config_description = "16K BPE tokenizer, trained on Wikitext"
    -- set ns.vocab_size = vocab_size | default(16000)
    -- set ns.model_max_length = model_max_length | default(32768)
