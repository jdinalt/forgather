-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.tokenizer_name = "wikitext_8k"
    -- set ns.config_name = "Wikitext 8K"
    -- set ns.config_description = "8K BPE tokenizer, trained on Wikitext"
    -- set ns.vocab_size = 8000
    -- set ns.model_max_length = 2048
-- endblock config_metadata