-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.tokenizer_name = "wikitext_2k"
    -- set ns.config_name = "Wikitext 2K"
    -- set ns.config_description = "2K BPE tokenizer, trained on Wikitext"
    -- set ns.vocab_size = 2000
    -- set ns.model_max_length = 2048
-- endblock config_metadata
