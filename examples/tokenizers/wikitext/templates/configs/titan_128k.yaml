-- extends 'project.yaml'

-- block config_metadata
    == super()
    ## Probably not optimal, but good enough for testings...
    -- set ns.tokenizer_name = "wikitext_titan_128k"
    -- set ns.config_name = "Torch Titan Compatible Wikitext 128K"
    -- set ns.config_description = "Torch Titan Compatible Wikitext 128K"
    -- set ns.vocab_size = 128256
    -- set ns.model_max_length = 131072
-- endblock config_metadata