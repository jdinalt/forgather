-- extends 'project.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "bfloat16r"
    -- set ns.config_description = "Train with bf16 weights"
    -- set ns.log_name = "bf16_adamw"
    -- set ns.model_name = "bf16_adamw"

[trainer_args]
    == super()
    # **bf16**
    default_dtype: "bfloat16"
    mixed_precision: null

[optimizer]
optimizer: &optimizer !partial:forgather.ml.optim:AdamW
    lr: {{ ns.global_lr | toyaml }}