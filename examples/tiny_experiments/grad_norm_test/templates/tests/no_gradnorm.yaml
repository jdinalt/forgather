-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Disable Gradient Norm"
    -- set ns.config_description = "Tests with gradient norm enabled disabled"
    -- set ns.log_name = "no_gradnorm"
-- endblock config_metadata


-- block trainer_definition
    -- include 'experiment.trainer_config'
-- endblock trainer_definition


-- block construct_new_model
    -- include 'experiment.model_config'
-- endblock construct_new_model

#-------------------- experiment.trainer_config --------------------
-- extends 'tiny.trainer_config'

-- block trainer_args
    == super()
    # Experiment overrides
    max_grad_norm: 0.0
    
-- endblock trainer_args

#-------------------- experiment.model_config --------------------
-- extends 'project.model_config'

-- block model_config
    == super()
    # Experiment overrides
<< endblock model_config
