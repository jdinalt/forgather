-- extends 'base_pipeline_project.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Interleaved Zero Bubble with 4 GPUs"
    -- set ns.config_description = "Pipeline with 4 GPUs"
    -- set ns.log_name = "izb_4gpu"
    -- set ns.trainer_class = 'trainers/pipeline_trainer.yaml'
    -- set ns.nproc_per_node = 4
    -- set ns.pipe_schedule_factory = "ScheduleInterleavedZeroBubble"

[trainer_args]
    == super()

    # Experiment Overrides
    n_microbatches: 8
    is_multistage: True
    stages_per_rank: 2
    per_device_train_batch_size: 8
    per_device_eval_batch_size: 8