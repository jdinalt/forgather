-- extends 'base_pipeline_project.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Interleaved 1F1B with 4 GPUs"
    -- set ns.config_description = "Pipeline with 4 GPUs"
    -- set ns.log_name = "i1f1b_4gpu"
    -- set ns.nproc_per_node = 4
    -- set ns.pipe_schedule_factory = "ScheduleInterleaved1F1B"

[trainer_args]
    == super()
    # Experiment Overrides
    n_microbatches: 8
    is_multistage: True
    stages_per_rank: 2