-- extends 'project.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Small Llama"
    -- set ns.config_description = "A Llama (pre-LN, RoPE, SiLU GLU, GQA; ~34M params)"
    -- set ns.model_name = "small_llama"
    -- set ns.model_project_dir = joinpath(ns.forgather_dir, 'examples', 'models', 'llama')
    -- set ns.model_project_config = "small.yaml"
