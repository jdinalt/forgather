-- extends "configs/unified.yaml"

[config_metadata]
    == super()
    -- set ns.config_name = "Unified SinkGD Nesterov"
    -- set ns.config_description = "SinkGD with Nesterov momentum=0.9"
    -- set ns.log_name = log_name | default("nesterov")
    -- set ns.momentum = 0.9
    -- set ns.nesterov = True
