-- extends "configs/unified.yaml"

[config_metadata]
    == super()
    -- set ns.config_name = "Unified SinkGD GA=4"
    -- set ns.config_description = "Gradient accumulation 4 (64K tokens/step)"
    -- set ns.log_name = log_name | default("ga-4")
    -- set ns.gradient_accumulation_steps = 4
