-- extends "configs/unified.yaml"

[config_metadata]
    == super()
    -- set ns.config_name = "Unified SinkGD (col_only embed)"
    -- set ns.config_description = "SinkGD with col_only mode for embed/LM-head"
    -- set ns.log_name = log_name | default("unified-colonly")

[optim_embed_grp]
    == super()
    mode: "col_only"