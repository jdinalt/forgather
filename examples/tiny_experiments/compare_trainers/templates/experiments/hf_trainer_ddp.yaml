-- extends 'experiments/hf_trainer.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "HF Trainer DDP"
    -- set ns.config_description = "Train with transformers.Trainer implementation, with DDP"
    -- set ns.log_name = "hf_trainer_ddp"
    -- set ns.nproc_per_node = "gpu"