-- extends 'project.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "DDP Trainer"
    -- set ns.config_description = "Train with Forgather's native DDP trainer"
    -- set ns.log_name = "ddp_trainer"

    ## Set trainer; this is a variable defined in 'projects/tiny.yaml'
    -- set ns.trainer_class = 'trainers/ddp_trainer.yaml'

[trainer_args]
    == super()
    # Not supported by HF trainer.
    float32_matmul_precision: "high"

[trainer_definition]
    -- include 'ddp_trainer.trainer_config'

#-------------------- ddp_trainer.trainer_config --------------------
-- extends 'project.trainer_config'

[trainer_meta_config]
    == super()
    -- set ns.nproc_per_node = "gpu"
