-- extends 'experiments/amp/fg_adafactor.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "FG Adafactor-8"
    -- set ns.config_description = "Forgather Adafactor Implementation w/ 8-grad accumulation steps"
    -- set ns.log_name = log_name | default("fg_adafactor-8")
    -- set ns.gradient_accumulation_steps = 8
