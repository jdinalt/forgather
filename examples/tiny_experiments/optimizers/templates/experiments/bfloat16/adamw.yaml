-- extends 'bfloat16.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Torch AdamW bf16"
    -- set ns.config_description = "Torch AdamW; the baseline control for bf16"
    -- set ns.log_name = log_name | default("adamw_bf16")

[optimizer]
optimizer: &optimizer !partial:torch:optim.AdamW
    lr: {{ ns.global_lr | toyaml }}