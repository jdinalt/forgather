-- extends 'experiments/amp/adamw.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Torch AdamW-32"
    -- set ns.config_description = "AdamW with 32x gradient accumulation (eff. batch=1024), scaled beta2"
    -- set ns.log_name = log_name | default("adamw-32")
    -- set ns.gradient_accumulation_steps = 32
