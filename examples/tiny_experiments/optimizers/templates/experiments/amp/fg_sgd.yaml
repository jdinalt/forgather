-- extends 'project.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "FG SGD"
    -- set ns.config_description = "Forgather SGD Implementation"
    -- set ns.log_name = log_name | default("fg_sgd")
    -- set ns.lr_alpha = 1.0
    -- set ns.base_lr = lr | default(1e0)

[optimizer]
optimizer: &optimizer !partial:forgather.ml.optim:SGD
    lr: {{ ns.global_lr | toyaml }}