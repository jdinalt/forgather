-- extends 'project.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "SGD"
    -- set ns.config_description = "Torch SGD"
    -- set ns.log_name = log_name | default("sgd")
    -- set ns.lr_alpha = 1.0

[optimizer]
optimizer: &optimizer !lambda:torch:optim.SGD
    lr: {{ ns.global_lr | toyaml }}
