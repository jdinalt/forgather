-- extends 'project.yaml'
## Gradient Multi-Normalization for Stateless and Scalable LLM Training
## https://arxiv.org/pdf/2502.06742
[config_metadata]
    == super()
    -- set ns.config_name = "SinkGD"
    -- set ns.config_description = "SinkGD"
    -- set ns.log_name = log_name | default("sinkgd")
    -- set ns.sinkgd_lr = sinkgd_lr | default(0.02)
    -- set ns.sinkgd_alpha = sinkgd_alpha | default(0.05)
    -- set ns.base_lr = base_lr | default(5.0e-2)

[variable_listing]
    == super()
# ns.sinkgd_lr: {{ ns.sinkgd_lr }}
# ns.sinkgd_alpha: {{ ns.sinkgd_alpha }}

[optimizer]
optimizer: &optimizer !partial:forgather.ml.optim.multiopt:make_re_multiopt
    optimizer_map:
        - [ "embed|lm_head|norm|bias", "adam" ]
        - [ ".*", "sinkgd" ]
    factories:
        sinkgd: !partial:forgather.ml.optim.sinkgd_exp:SinkGD
            lr: {{ ns.effective_lr | toyaml }}
            clip_threshold: 0.5
            #normalize_output: False
            #weight_decay: !!float 0.0
        adam: !partial:torch.optim:Adam
            lr: {{ ns.sinkgd_lr | toyaml }}
            weight_decay: !!float 0.0
    debug: True

[dynamic_args]
    == super()
    sinkgd_lr:
        names: "--sinkgd-lr"
        type: float
        help: "SinkGD Global LR"
    sinkgd_alpha:
        names: "--sinkgd-alpha"
        type: float
        help: "SinkGD Alpha"

