-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Tiny Deep One"
    -- set ns.config_description = "A tiny Deep One model."
    -- set ns.model_name = "tiny_deepone"
-- endblock config_metadata


-- block construct_new_model
    -- include 'experiment.model_config'
-- endblock construct_new_model

#-------------------- experiment.model_config --------------------
-- extends 'models/tiny/tiny_deepone.yaml'