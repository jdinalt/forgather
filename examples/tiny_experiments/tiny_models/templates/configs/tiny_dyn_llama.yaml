-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Tiny Dynamic Llama"
    -- set ns.config_description = "A tiny dynamic llama model"
    -- set ns.model_name = "tiny_dllama"
-- endblock config_metadata


-- block construct_new_model
    -- include 'experiment.model_config'
-- endblock construct_new_model

#-------------------- experiment.model_config --------------------
-- extends 'models/tiny/tiny_dyn_llama.yaml'