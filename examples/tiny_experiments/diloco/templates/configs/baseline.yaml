-- extends "projects/tinyv2.yaml"

## Non-DiLoCo baseline: same tinyv2 hyperparameters as the DiLoCo
## configs, for comparison. Lets you see how a vanilla single-node
## run scores against an N-worker DiLoCo run on the same setup.

[config_metadata]
    == super()
    -- set ns.config_name = "Baseline"
    -- set ns.config_description = "Tiny Experiment baseline, for comparison"
    -- set ns.log_name = "baseline"
