-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "SwiGLU"
    -- set ns.config_description = "SwiGLU feedforward layer"
    -- set ns.model_name = "swi_glu"
    -- set ns.log_name = "swi_glu"
-- endblock config_metadata


-- block construct_new_model
    -- include 'models/swi_glu.yaml'
-- endblock construct_new_model
