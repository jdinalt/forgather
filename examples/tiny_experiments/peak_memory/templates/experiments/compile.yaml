-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Compile"
    -- set ns.config_description = "Torch compile"
    -- set ns.model_name = "control"
    -- set ns.log_name = "compile"
-- endblock config_metadata

-- block trainer_definition
    -- include 'experiment.trainer_config'
-- endblock trainer_definition

#-------------------- experiment.trainer_config --------------------
-- extends 'project.trainer_config'


-- block trainer_args
    == super()
    # experiment overrides
    torch_compile: True
    torch_compile_dynamic: False
-- endblock trainer_args
