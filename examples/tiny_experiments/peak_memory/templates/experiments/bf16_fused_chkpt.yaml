-- extends 'project.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Bf16 Fused Ckpt"
    -- set ns.config_description = "bfloat16, activation checkpoints, and fused backward/optimizer step"
    -- set ns.model_name = "checkpoint"
    -- set ns.log_name = "bf16_fused_ckpt"

[trainer_definition]
    -- include 'experiment.trainer_config'

#-------------------- experiment.trainer_config --------------------
-- extends 'project.trainer_config'

[trainer_args]
    == super()
    # experiment overrides
    gradient_checkpointing: True
    fuse_optim_with_backward: True
    default_dtype: bfloat16