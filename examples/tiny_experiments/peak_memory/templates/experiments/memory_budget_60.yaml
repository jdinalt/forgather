-- extends 'project.yaml'

[config_metadata]
    == super()
    ## See https://github.com/pytorch/pytorch/blob/v2.8.0/torch/_functorch/config.py
    -- set ns.config_name = "Memory Budget 0.60"
    -- set ns.config_description = "Torch compile with activation_memory_budget = 0.60"
    -- set ns.model_name = "control"
    -- set ns.log_name = "mem_budget_60"

[trainer_args]
    == super()
    # experiment overrides
    torch_compile: True
    torch_compile_dynamic: False
    activation_memory_budget: 0.60
