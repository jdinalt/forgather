-- extends 'project.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Bfloat16"
    -- set ns.config_description = "Train using BFlaot16 format"
    -- set ns.model_name = "control"
    -- set ns.log_name = "bfloat16"

[trainer_args]
    == super()
    # experiment overrides
    fuse_optim_with_backward: True
    default_dtype: bfloat16