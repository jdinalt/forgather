-- extends 'project.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Control"
    -- set ns.config_description = "Baseline 32bit model"
    -- set ns.model_name = "control"
    -- set ns.log_name = "control"
