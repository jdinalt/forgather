-- extends 'project.yaml'

[config_metadata]
    == super()
    -- set ns.config_name = "Bf16 Fused Ckpt Mem"
    -- set ns.config_description = "bfloat16, activation checkpoints, and fused backward/optimizer step, memeory budget 0.60"
    -- set ns.model_name = "control"
    -- set ns.log_name = "bf16_fused_ckpt_60"

[trainer_definition]
    -- include 'experiment.trainer_config'

#-------------------- experiment.trainer_config --------------------
-- extends 'project.trainer_config'

[trainer_args]
    == super()
    # experiment overrides
    torch_compile: True
    torch_compile_dynamic: False
    activation_memory_budget: 0.60
    default_dtype: bfloat16