-- extends 'project.yaml'

-- block config_metadata
    == super()
    -- set ns.config_name = "Fused Checkpoint"
    -- set ns.config_description = "Fuse optimizer with backward and activation checkpoint"
    -- set ns.model_name = "checkpoint"
    -- set ns.log_name = "fused_checkpoint"
-- endblock config_metadata


-- block trainer_definition
    -- include 'experiment.trainer_config'
-- endblock trainer_definition

#-------------------- experiment.trainer_config --------------------
-- extends 'project.trainer_config'


-- block trainer_args
    == super()
    # experiment overrides
    gradient_checkpointing: True
    fuse_optim_with_backward: True
-- endblock trainer_args
