-- extends 'project.yaml'

-- block experiment_metadata
    == super()
    -- set ns.EXPERIMENT_NAME = "HF Trainer"
    -- set ns.EXPERIMENT_DESCRIPTION = "Train with transformers.Trainer implementation"
    -- set ns.LOG_NAME = "hf_trainer"

    ## Set trainer
    -- set ns.trainer_class = 'trainers/hf_trainer.yaml'
-- endblock experiment_metadata