-- extends 'project.yaml'

-- block experiment_metadata
    == super()
    -- set ns.EXPERIMENT_NAME = "Accelerate Trainer"
    -- set ns.EXPERIMENT_DESCRIPTION = "Train with aiws.accel_trainer.AccelTrainer implementation"
    -- set ns.LOG_NAME = "accel_trainer"

    ## Set trainer
    -- set ns.trainer_class = 'trainers/accel_trainer.yaml'
-- endblock experiment_metadata