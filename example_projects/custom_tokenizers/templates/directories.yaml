## This definition is used to boot-strap the path definitions,
##  so it does not have access to the template library -- it's used to find it! 

## Project specific paths
-- set ns.PROJECT_DIR = project_directory
-- set ns.ASSETS_DIR = normpath(path_join(ns.PROJECT_DIR, '..', '..'))
-- set ns.PROJ_TEMPLATES_DIR = path_join(ns.PROJECT_DIR, 'templates')
-- set ns.TEMPLATES_DIR = path_join(ns.ASSETS_DIR, 'templates')
-- set ns.EXPERIMENT_DIR = path_join(ns.PROJ_TEMPLATES_DIR, 'experiments')
-- set ns.SOURCE_DIR = path_join(ns.ASSETS_DIR, 'src')
-- set ns.MODELS_DIR = path_join(ns.PROJECT_DIR, 'output_models')

## Save tokenizers in output directory
-- set ns.TOKENIZERS_DIR = path_join(ns.MODELS_DIR, 'tokenizers')
-- set ns.DATASETS_DIR = path_join(ns.ASSETS_DIR, 'datasets')
-- set ns.SCRIPTS_DIR = path_join(ns.ASSETS_DIR, 'scripts')
-- set ns.MODEL_SOURCE_DIR = path_join(ns.ASSETS_DIR, 'model_zoo')