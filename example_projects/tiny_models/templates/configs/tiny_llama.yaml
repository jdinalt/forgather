-- extends 'project.yaml'

-- block experiment_metadata
    == super()
    -- set ns.EXPERIMENT_NAME = "Tiny LLama"
    -- set ns.EXPERIMENT_DESCRIPTION = "A tiny llama model."
    -- set ns.MODEL_NAME = "tiny_llama"
-- endblock experiment_metadata


-- block construct_new_model
    -- include 'experiment.model_config'
-- endblock construct_new_model

#-------------------- experiment.model_config --------------------
-- extends 'models/tiny/tiny_llama.yaml'