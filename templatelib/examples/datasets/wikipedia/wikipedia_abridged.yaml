-- extends "datasets/wikipedia/wikipedia.yaml"

-- block datasets_meta_config
    == super()
    -- set ns.datasets_name = "Wikepedia Dataset Abridged"
    -- set ns.datasets_description = "Small subset of Wikipedia dataset"
    -- set ns.datasets_source = "https://huggingface.co/datasets/wikimedia/wikipedia"
-- endblock datasets_meta_config

-- block train_dataset
    == super()
    select_range: 10000
    to_iterable: False
    seed: 42
<< endblock train_dataset

