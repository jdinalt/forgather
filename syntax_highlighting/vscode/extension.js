// Minimal extension entry point for Forgather syntax highlighting
// This extension only provides syntax highlighting via TextMate grammar,
// so no activation logic is needed.

function activate(context) {
    // No activation logic needed for pure syntax highlighting
}

function deactivate() {
    // No deactivation logic needed
}

module.exports = {
    activate,
    deactivate
};