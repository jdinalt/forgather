-- extends "project.yaml"

-- block config_vars
    == super()
    -- set ns.config_name = "Wider Model"
    -- set ns.config_description = "Base configuration + larger hidden dimension"
    -- set ns.hidden_dim = 1024
<< endblock config_vars

