-- extends "project.yaml"

-- block config_vars
    == super()
    -- set ns.config_name = "Adam Optimizer"
    -- set ns.config_description = "Base configuration + Adam optimizer"
<< endblock config_vars


-- block optimizer
.define: &optimizer !singleton:torch.optim:Adam
    args: [ *model_params ]
    kwargs:
        lr: {{ ns.lr }}
<< endblock optimizer